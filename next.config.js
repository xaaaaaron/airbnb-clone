/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["media.designrush.com", "rare-gallery.com" , "links.papareact.com"]
  },
}

module.exports = nextConfig
